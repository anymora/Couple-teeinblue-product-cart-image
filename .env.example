# Server
PORT=3000

# Security: allow only specific image hostnames (comma-separated)
# For TeeInBlue typical:
ALLOWED_HOSTS=cdn.teeinblue.com

# Output defaults (cart thumbnails often square-ish; adjust if needed)
DEFAULT_WIDTH=700
DEFAULT_HEIGHT=700

# Crop behavior
# How much to "cut" from the opposite side (0.30 = 30%)
CUT_PERCENT=0.30

# How much to zoom in (>1 = zoom in)
ZOOM=1.20

# JPEG quality (1-100)
JPEG_QUALITY=85

# Fetch safeguards
FETCH_TIMEOUT_MS=8000
MAX_IMAGE_BYTES=15000000
